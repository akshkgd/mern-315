<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Codekaro Login Page</title>
    <style>
        body{
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }
        .container{
            width: 300px;
            margin: 70px auto;
            /* text-align: center; */
        }
        input{
            width: 100%;
            padding: 16px;
            box-sizing: border-box;
            font-size: 16px;
            margin-bottom: 20px;
            border:1px solid lightgray;
            border-radius: 10px;
        }
        .btn{
            padding: 18px 44px;
            background-color: black;
            border: none;
            border-radius: 80px;
            box-sizing: border-box;
            color: white;
            font-size: 16px;
            display: inline-block;
            width: 100%;
        }
        .wrong{
            border: 1px solid red;
        }
        label{
            color: red;
            margin-bottom: 6px;
            display: inline-block;
        }
    </style>
</head>
<body>
    <form action="/login" method="post">
        <% if(status === true){  %>
        <div class="container">
            <h1>Welcome Back!</h1>
            <input type="email" name="email" class="email" placeholder="Enter your Email">
            <input type="password" name="password" class="password" placeholder="Enter your password">
            <input type="submit" class="btn" value="Login Now" />
        </div>
        <% } else {%>
            <div class="container">
                <h3>Email or password is wrong!</h3>
                <label for="">Email is incorrect</label>
                <input type="email" name="email" class="wrong" placeholder="Enter your Email">
                <label for="">Password is incorrect</label>
                <input type="password" name="password" class="wrong password" placeholder="Enter your password">
                <input type="submit" class="btn" value="Login Now" />
            </div>
        <% } %>

    </form>
    
</body>
</html>