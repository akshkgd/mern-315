<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Node js intro</title>

    <style>
        *{
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            
        }
        .bg-card{
            border:none;
            text-align: center;
            background-color: #e6fee5;
            padding: 40px;
            margin: 50px auto;
            width: 530px;
            border-radius: 16px;
        }
        .green{
            color:green;
        }
        .card-img{
            width: 100%;
            border-radius: 16px;
        }
        .input{
            width: 100%;
            border-radius: 8px;
            padding: 16px;
            margin: 10px auto;
            box-sizing: border-box;
            border: 1px solid lightgray;
        }
        .input:focus{
            outline: none;
            border:1px solid rgb(0, 128, 255);
        }
        .btn{
            border-radius: 8px;
            padding: 16px 36px;
            box-sizing: border-box;
            border: none;
            background-color: black;
            color: white;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="bg-card">
        <h1>Hi <%= user %> , you have successfully enrolled for the Web Development Bootcamp with email id as <span class="green"><%=email %></span></h1>
    </div>
</body>
</html>